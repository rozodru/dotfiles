{
  "layer": "top",
  "position": "top",
  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["custom/cmus","clock"],
  "modules-right": ["disk","cpu", "memory", "battery", "network", "pulseaudio", "tray"],

  "clock": {
    "format": "{:%a %d %b  %H:%M}"
  },
  "cpu": {
    "format": "CPU {usage}%"
  },
  "memory": {
    "format": "MEM {used}MB"
  },
  "battery": {
    "format": "BAT {capacity}% {icon}"
  },
  "network": {
    "format": "{ifname} {ipaddr}/{cidr}"
  },
  "pulseaudio": {
    "format": "VOL {volume}%"
  },
  "custom/cmus": {
      "exec": "~/.config/waybar/scripts/cmus_now.sh",
      "interval": 2
    },
  "disk": {
      "format": "DISK {used}/{total}",
    }
}

